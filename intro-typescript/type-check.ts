// tipagem dinamica
// Dynamic Typed 

/* 
  Já o typescript que é uma linguagem com sistema de tipagem estática,
  uma vez que declaramos uma variável que ela tem um tipo especifico como por exemplo 
  o código logo abaixo que é do tipo string, ou seja não posso mudar esse tipo, e essa restrição 
  ela é proporcional eu não posso mudar esse tipo. 
*/

let message = "Hello world";

// message = 8

console.log(message.toUpperCase())