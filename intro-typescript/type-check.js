// tipagem dinamica
// Dynamic Typed 

/* 
  Uma variavel que declaramos, ela pode mudar de tipo ao longo da execução do nosso programa
  e o tipo da variavel ele vai ser verificado, quando nosso programa é executado, ou seja, em tempo de 
  execução.
*/
let message = "Hello world";

message = 8

console.log(message.toUpperCase())

// console.log(typeof message)